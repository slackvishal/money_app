name: wallet
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_screenutil: ^0.5.2
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
      - assets/paying.png
      - assets/request.png
      - assets/send.png
      - assets/topup.png

  fonts:
         - family: Rix

           fonts:
             - asset: assets/rix.ttf

